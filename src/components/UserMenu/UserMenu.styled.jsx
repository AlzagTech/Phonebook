import styled from 'styled-components';

export const UserMenuBox = styled.div`
  display: flex;
  align-items: center;

  p {
    margin-right: 10px;
  }

  p span {
    font-weight: bold;

    text-transform: capitalize;
  }
`;
